import type { Metadata } from "next";
export const metadata: Metadata = { title: "Typing Speed Test — Check Your WPM", description: "Test your typing speed and accuracy online with the free SmartEdgeTools typing speed test.", alternates: { canonical: "/typing-test" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
