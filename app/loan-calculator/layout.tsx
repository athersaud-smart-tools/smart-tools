import type { Metadata } from "next";
export const metadata: Metadata = { title: "Loan Calculator — Estimate Monthly Payments", description: "Estimate loan payments, interest, and repayment amounts with the free SmartEdgeTools loan calculator.", alternates: { canonical: "/loan-calculator" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
