import type { Metadata } from "next";
export const metadata: Metadata = { title: "Color Picker — Get HEX and RGB Colors", description: "Pick colors online and get useful HEX and RGB values with the free SmartEdgeTools color picker.", alternates: { canonical: "/color-picker" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
