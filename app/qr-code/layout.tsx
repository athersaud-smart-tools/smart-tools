import type { Metadata } from "next";
export const metadata: Metadata = { title: "QR Code Generator — Create QR Codes Online", description: "Create QR codes online from text or links with the free SmartEdgeTools QR code generator.", alternates: { canonical: "/qr-code" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
