import type { Metadata } from "next";
export const metadata: Metadata = { title: "Percentage Calculator — Calculate Percentages", description: "Calculate percentages, percentage increases, decreases, and common percentage problems quickly with the free SmartEdgeTools calculator.", alternates: { canonical: "/percentage-calculator" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
