import type { Metadata } from "next";
export const metadata: Metadata = { title: "Image Resizer — Resize Images Online", description: "Resize images online to the dimensions you need with the free SmartEdgeTools image resizer.", alternates: { canonical: "/image/resize" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
