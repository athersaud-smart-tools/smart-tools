import type { Metadata } from "next";
export const metadata: Metadata = { title: "Image Compressor — Compress Images Online", description: "Compress JPG, PNG, and other images online to reduce file size with the free SmartEdgeTools image compressor.", alternates: { canonical: "/image/compress" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
