import type { Metadata } from "next";
export const metadata: Metadata = { title: "Unit Converter — Convert Length, Weight and More", description: "Convert common units including length, weight, and temperature quickly with the free SmartEdgeTools unit converter.", alternates: { canonical: "/unit-converter" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
