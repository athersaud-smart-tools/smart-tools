import type { Metadata } from "next";
export const metadata: Metadata = { title: "Word Counter — Count Words and Characters", description: "Count words, characters, and text quickly with the free SmartEdgeTools online word counter.", alternates: { canonical: "/word-counter" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
