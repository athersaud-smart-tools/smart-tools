import type { Metadata } from "next";
export const metadata: Metadata = { title: "Case Converter — UPPER, lower and Title Case", description: "Convert text between uppercase, lowercase, title case, and other common formats with the free SmartEdgeTools case converter.", alternates: { canonical: "/text-case" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
