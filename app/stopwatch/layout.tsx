import type { Metadata } from "next";
export const metadata: Metadata = { title: "Online Stopwatch — Free Lap Timer", description: "Use a simple free online stopwatch with lap timing from SmartEdgeTools on desktop or mobile.", alternates: { canonical: "/stopwatch" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
